<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recuperación de Contraseña - MetelePlay</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 0;
            background-color: #f8f9fa;
        }
        .email-container {
            background-color: #ffffff;
            margin: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }
        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            padding: 30px 20px;
            text-align: center;
        }
        .logo {
            max-width: 150px;
            height: auto;
            margin-bottom: 15px;
        }
        .header h1 {
            color: #ffffff;
            margin: 0;
            font-size: 24px;
            font-weight: 600;
        }
        .content {
            padding: 40px 30px;
        }
        .greeting {
            font-size: 18px;
            color: #2c3e50;
            margin-bottom: 20px;
        }
        .recovery-code {
            background: linear-gradient(135deg, #ffeaa7 0%, #fab1a0 100%);
            padding: 25px;
            border-radius: 8px;
            text-align: center;
            margin: 30px 0;
            border-left: 4px solid #e17055;
        }
        .recovery-code h2 {
            margin: 0 0 10px 0;
            color: #2d3436;
            font-size: 16px;
            font-weight: 600;
        }
        .code-display {
            font-size: 36px;
            font-weight: bold;
            color: #2d3436;
            letter-spacing: 8px;
            margin: 15px 0;
            font-family: 'Courier New', monospace;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.1);
        }
        .expires {
            font-size: 14px;
            color: #636e72;
            margin-top: 10px;
        }
        .cta-button {
            display: inline-block;
            background: linear-gradient(135deg, #00b894 0%, #00cec9 100%);
            color: #ffffff !important;
            padding: 15px 30px;
            text-decoration: none;
            border-radius: 25px;
            font-weight: 600;
            margin: 20px 0;
            transition: transform 0.3s ease;
            box-shadow: 0 4px 15px rgba(0, 184, 148, 0.3);
        }
        .cta-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(0, 184, 148, 0.4);
        }
        .security-notice {
            background-color: #fff3cd;
            border: 1px solid #ffeaa7;
            border-left: 4px solid #fdcb6e;
            padding: 20px;
            border-radius: 5px;
            margin: 30px 0;
        }
        .security-notice h3 {
            color: #8d5524;
            margin-top: 0;
            display: flex;
            align-items: center;
        }
        .security-notice ul {
            color: #8d5524;
            margin: 10px 0;
            padding-left: 20px;
        }
        .footer {
            background-color: #2d3436;
            color: #ddd;
            padding: 30px;
            text-align: center;
            font-size: 14px;
        }
        .footer a {
            color: #74b9ff;
            text-decoration: none;
        }
        .social-links {
            margin: 20px 0;
        }
        .social-links a {
            display: inline-block;
            margin: 0 10px;
            color: #74b9ff;
            text-decoration: none;
        }
        @media (max-width: 600px) {
            .email-container {
                margin: 10px;
            }
            .content {
                padding: 20px 15px;
            }
            .code-display {
                font-size: 28px;
                letter-spacing: 4px;
            }
        }
    </style>
</head>
<body>
    <div class="email-container">
        <!-- Header -->
        <div class="header">
            <img src="https://gamsgo.oss-us-east-1.aliyuncs.com/GamsGo.png" alt="MetelePlay" class="logo">
            <h1>🔐 Recuperación de Contraseña</h1>
        </div>

        <!-- Content -->
        <div class="content">
            <div class="greeting">
                <strong>¡Hola {{userName}}!</strong>
            </div>
            
            <p>Hemos recibido una solicitud para restablecer la contraseña de tu cuenta en <strong>MetelePlay</strong>.</p>
            
            <p>Si no solicitaste este cambio, puedes ignorar este correo de forma segura. Tu contraseña no será modificada.</p>

            <!-- Recovery Code -->
            <div class="recovery-code">
                <h2>🔑 Tu código de recuperación es:</h2>
                <div class="code-display">{{recoveryCode}}</div>
                <div class="expires">⏰ Válido por {{expiresIn}}</div>
            </div>

            <!-- CTA Button -->
            <div style="text-align: center;">
                <a href="{{verificationUrl}}" class="cta-button">
                    🔓 Restablecer mi contraseña
                </a>
            </div>

            <p style="text-align: center; margin-top: 15px; font-size: 14px; color: #666;">
                También puedes copiar y pegar este enlace en tu navegador:<br>
                <a href="{{verificationUrl}}" style="color: #667eea; word-break: break-all;">{{verificationUrl}}</a>
            </p>

            <!-- Security Notice -->
            <div class="security-notice">
                <h3>⚠️ Importante - Medidas de Seguridad</h3>
                <ul>
                    <li>Este código es válido solo por <strong>{{expiresIn}}</strong></li>
                    <li>No compartas este código con nadie</li>
                    <li>Si no solicitaste este cambio, ignora este email</li>
                    <li>Usa una contraseña segura con al menos 8 caracteres</li>
                    <li>Incluye mayúsculas, minúsculas, números y símbolos</li>
                </ul>
            </div>

            <p style="margin-top: 30px;">
                Si tienes problemas con el enlace, contacta a nuestro equipo de soporte.
            </p>

            <p style="color: #666; font-style: italic;">
                Este es un email automático, por favor no respondas a esta dirección.
            </p>
        </div>

        <!-- Footer -->
        <div class="footer">
            <h3 style="margin-top: 0; color: #fff;">MetelePlay</h3>
            <p>Tu plataforma de entretenimiento digital de confianza</p>
            
            <div class="social-links">
                <a href="{{frontendUrl}}">🌐 Sitio Web</a>
                <a href="{{frontendUrl}}/support">📞 Soporte</a>
                <a href="{{frontendUrl}}/privacy">🛡️ Privacidad</a>
            </div>
            
            <p style="margin-bottom: 0; color: #999;">
                © {{currentYear}} MetelePlay. Todos los derechos reservados.
            </p>
        </div>
    </div>
</body>
</html>
